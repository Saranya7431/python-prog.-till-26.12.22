n=int(input('enter the size'))
a=list(map(int,input().split()))
i=0
for i in range(i,n):
    for j in range(i+1,n):
        if a[i]>a[j]:
            temp=a[i]
            a[i]=a[j]
            a[j]=temp
print(a)
